// Re-export wagmiConfig and enabledChains from appkit for unified config
// This ensures all components use the same wagmiConfig that AppKit uses
export { wagmiConfig, enabledChains } from '../appkit.js';
